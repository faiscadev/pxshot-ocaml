; Examples - excluded from package build
; To build examples locally: dune build --root . examples/

(executable
 (name basic)
 (libraries pxshot lwt lwt.unix))

(executable
 (name stored)
 (libraries pxshot lwt lwt.unix))

(executable
 (name usage_example)
 (libraries pxshot lwt lwt.unix))
